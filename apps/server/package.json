{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.mjs"
  },
  "dependencies": {
    "@ai-sdk/devtools": "^1.0.1",
    "@ai-sdk/google": "^4.0.7",
    "@all-chat/db": "workspace:*",
    "@all-chat/env": "workspace:*",
    "ai": "catalog:",
    "anonymous-names": "^0.0.10",
    "cors": "^2.8.6",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "socket.io": "^4.8.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@all-chat/config": "workspace:*",
    "@all-chat/db": "workspace:*",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "tsdown": "^0.22.3",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
